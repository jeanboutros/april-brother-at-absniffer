# B2: APPLY — Fix VTIME integer division truncation

| Field | Value |
|-------|-------|
| Agent | code-architect |
| Timestamp | 2026-06-13T12:00:00Z |
| Step | B2-1 |

### B2-1: APPLY Unit 1 — Static asserts + explicit cast

| Unit | 1 |
| Build result | PASS — exit 0, 0 warnings |
| Files changed | `include/ble_sniffer/bluetooth_at_driver.h` (lines 47-54), `src/bluetooth_at_driver.cpp` (line 70) |

#### Changes

**bluetooth_at_driver.h** — Updated Doxygen on `sniffer_timeout` and added two `static_assert`:
```cpp
/// Read timeout in milliseconds before returning NO_DATA.
/// @note Range constraint: 100ms–25500ms (25.5s). VTIME is an unsigned char
///       representing tenths of a second (0–255). Values below 100ms would
///       yield VTIME=0 (immediate return), and values above 25500ms would
///       overflow cc_t.
constexpr int sniffer_timeout = 1000;
static_assert(sniffer_timeout >= 100, "VTIME requires timeout >= 100ms");
static_assert(sniffer_timeout / 100 <= 255, "VTIME exceeds cc_t max (25.5s)");
```

**bluetooth_at_driver.cpp** — Changed VTIME assignment to use explicit cast:
```cpp
tty.c_cc[VTIME] = static_cast<cc_t>(sniffer_timeout / 100);
```

### B3: VALIDATE

| Full build | PASS — exit 0, 0 warnings |
| AC coverage | 5/5 acceptance criteria satisfied |

#### Acceptance Criteria Verification

| AC# | Criterion | Status |
|-----|-----------|--------|
| 1 | `static_assert(sniffer_timeout >= 100, ...)` present | PASS |
| 2 | `static_assert(sniffer_timeout / 100 <= 255, ...)` present | PASS |
| 3 | VTIME assignment uses `static_cast<cc_t>(...)` | PASS |
| 4 | Doxygen documents 100ms–25.5s range constraint | PASS |
| 5 | Build passes with zero warnings | PASS |